# Storage

Where to put the application's useful files is here.

The filesystem folder is where the general files are placed.

The database folder is where the data files uploaded in the backoffice are placed; the image and file fields 
are saved here.

The [Storage](https://doc.netuno.org/docs/library/resources/storage) polyglot resource is used to access these files.
